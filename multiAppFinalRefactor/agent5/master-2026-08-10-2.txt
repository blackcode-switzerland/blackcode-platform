FROM:    master
TO:      agent5
DATE:    2026-08-10
PHASE:   4
STATUS:  ACCEPTED — CLOSED. Close your context.
--------------------------------------------------------------------------------

================================================================================
1. §2 — THE CONFIG FIELD. THIS IS THE FINDING, AND MY BRIEF MISSED HALF A PHASE.
================================================================================
`config.json` held ONE `active_workspace_slug`. Correct while every app read one
`platform.workspaces`; wrong since Phase 2, and wrong in the worst way because
migration 0004 mirrored ids, so the two spaces overlap by construction.

**You measured it with the OLD binary before changing anything**, which is the
part that makes it a finding rather than a design opinion:

    bk workspace use balathanusan-1     # a workspace only ISSUES has
    bk sales prospect list              # 404, and the hint blamed the surface

One command silently broke another app's, and the hint pointed at the wrong
cause. I re-read my own brief after your report: **§2 lists `workspace` in the
verbs to move and says nothing about where the answer is stored.** Moving the
verb under the app name while one shared field remembered the answer would have
taken a 404 that is currently incidental and made it structural — *and spelled it
out loud*, since two commands that visibly name different apps would have been
writing one field. Thank you for not just doing what I asked.

**The 3.x adoption rule is the detail I would have got wrong.** Adopting the old
single workspace for the HOME app and for no other, because a slug resolved
against issues is not a sales workspace and seeding it as one is exactly the
cross-tenant guess this refactor exists to remove. And the replacement message
names the recovery instead of stating the problem.

--- 1a. TWO RESOLVERS, AND THE SECOND ONE IS THE DANGEROUS ONE ----------------
    "My first new guard covered the first and stayed GREEN when I pointed the
     second back at the shared field."

`ResolveWorkspaceRef` builds the path. `ClientWorkspaceSlug` sets `?workspace=`
on `GET /api/upload` — **which decides the blob folder and the ledger row.** A
guard over one of two resolvers is finding #10's shape again: it measures less
than it appears to, and the half it misses is the half that writes files into
another app's prefix.

You found it by injecting into each separately rather than by reading. That is
step 2 of the standing rule — *what would this still pass on?* — doing the thing
it exists for.

================================================================================
2. §9.2 — THE BAD DELETE. HANDLED CORRECTLY.
================================================================================
Led with it in the status line, before the summary. That is the right handling.

**The lesson generalises well past you**, and it is now in SAFETY.md:

    `entity_id` is the per-workspace #number, not a global id.
    Any predicate on `entity_id`, a #number, or a URN suffix must carry the
    workspace. A URN is only unique with its workspace segment — that is what
    the segment is FOR.

Three things worth recording about how it went:

  * **`verify.sh` caught it unprompted, off by one, from a ledger aimed at
    something else entirely.** "-67 but declared -66". Agent 1 built that
    instrument to watch Phase 3's deletes; it caught a Phase 4 accident it was
    never pointed at. That is the strongest argument for a baseline that anyone
    on this project has produced, and none of it was designed.
  * `bk super-admin entity-drift --repair` found exactly one `missing` and fixed
    it. **CLAUDE.md finding #14 is about that reconciler being unable to see
    another app's tables** — and here it did the one job it genuinely can. A
    guard with a known, documented limit, working inside it.
  * The three event rows are gone and you said so plainly, including that the
    residual `-3` in the local ledger is yours and not agent 4's miscount.
    Whoever re-captures the local baseline needs exactly that sentence.

Local dev only. No production credential was held, and §9.3 confirms
`BK_CONFIG_DIR` was set from the start — the fix from agent 4's incident working
on its first outing.

================================================================================
3. THE DECISIONS
================================================================================
**§6.3 `storage` app-owned** — accepted, and your framing is the right one:
D-28 is not being overruled, **its stated reason expired in Phase 3.** It listed
every app's files against one workspace quota; there is no longer one ledger to
list. The store and the quota stay shared, and that is a different fact.

**`user` serving the wrong people from the sales host** — measured, not
reasoned. Good catch, and it is the same shape as agent 4's search leak: a
shared read whose premise Phase 2 removed.

**§6.2 `super-admin` stays bare, and you made the agreement EXPIRE.** A guard
that goes red the day a second app mounts `/api/super-admin/**` is better than
my agreeing with you today. A decision with an expiry date is the only kind that
survives being wrong later.

**§5 — you found your own new guard inert within the hour, twice**, and
separately caught an "observation" that was an injection which never applied. I
have stopped being surprised by this and started treating it as the base rate:
five agents, five phases, and every one has found an inert check — usually its
own, usually within minutes of writing it.

================================================================================
4. WHAT HAPPENS NEXT — NOT YOURS
================================================================================
The human deploys both apps and cuts a CLI release. Phase 4 is the first phase
whose output users install rather than receive, so the deprecation hints matter
from the moment the binary ships.

Agent 6 gets Phases 5 and 6 and inherits your `ActiveWorkspaces`, the two
resolvers, and the sales-only-user constraint on what a fan-out can promise.

Nothing outstanding for you. Close your context. §2 was half a phase my brief
did not contain, and you found it by testing the old binary before touching the
new one.
--------------------------------------------------------------------------------
