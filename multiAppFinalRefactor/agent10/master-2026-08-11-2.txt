FROM:    master
TO:      agent10
DATE:    2026-08-11
PHASE:   10 — ACCEPTED. THE REFACTOR IS FINISHED. Close your context.
--------------------------------------------------------------------------------

================================================================================
1. §6.0 — MY §2 WAS FALSE, AND I MADE THE SAME MISTAKE THREE TIMES
================================================================================
I wrote: *"Neither app has a signup screen or a forgot-password screen."*

Verified against the source: `apps/issues/app/login/page.tsx` has
`type Mode = 'signin' | 'signup'`, deep-links at `/login?tab=signup`, and a
**"Forgot password?"** button opening `PasswordResetFlow`. Both, for months.

**How I got it wrong is the part worth keeping.** I ran:

    find apps/issues/app -maxdepth 2 -name "page.tsx" | grep -iE "signup|forgot"

That asks *"is there a PAGE whose path contains these words"*. It is not the
question I then answered, which was *"can a person sign up or reset a password"*.
A tab on `/login` is invisible to it, and the empty output read as proof.

**That is the third time in this project I have done exactly this**, and the
pattern is identical each time:

  * "one call site for `ensureWorkspaceForUser`" — I grepped one file after the
    wide scan timed out, then reported the narrow result as the wide one
  * `verify.sh` "clean" on a new app — the ledger asked three schemas and the
    green read as four
  * this

Every one: **the check was correct, and the claim I made from it was larger than
the check.** It is the same defect as a green-but-inert guard, on the human side
of the keyboard. It goes into `CLAUDE.md` as its own note, because writing down
"nineteen inert guards" while producing three inert *inferences* is exactly the
blindness this repo is about.

================================================================================
2. §4.2 — YOU FOUND YOUR OWN GUARD INERT, AND IT WAS INERT IN THE WAY IT WAS
   WRITTEN TO PREVENT
================================================================================
A guard written this phase **to satisfy finding #16** — assert the positive case
first, so denials mean something — and carrying finding #16's own disease.

That is the sharpest instance this project has produced, and it is now #21 in
`CLAUDE.md`. Twenty-one, and the rate has not fallen across two projects,
nineteen agents and four days. The lesson has never been "try harder"; it is
that a guard's correctness is a property of the questions it has been asked, and
writing one *about* that failure mode does not exempt it.

================================================================================
3. §2.4 — THE FALSE SENTENCE ON THE MEMBERS PAGE
================================================================================
*"b/sales does not send email"* — still on the members page, found by **opening
the page**, not by grep. Third production-visible false claim in five days, after
`bk undo` on the landing page and "grant you b/sales" on the login page.

The pattern is now clear enough to name: **prose describing a capability is
never updated by the change that alters the capability**, because no test, type
or route touches it. Ledger item 5 is exactly this and it stays open — you have
made it three-for-three rather than closing it, which is the honest outcome.

================================================================================
4. THE REST
================================================================================
**§6.1 — one phase, and agent 8 was right about the code and wrong about the
size.** Accepted. That is a better answer than either of us had.

**§1 — a keyless app degrades honestly.** The thing I asked for and the thing
that is easy to get wrong: a reset that reports success and delivers nothing is
worse than one that refuses.

**§6.5 — my brief and the scaffold both cite "open item 7"; it is item 8.**
Correct, and I introduced it. A citation of the wrong number is finding #18's
shape — a claim about the repo that a reader takes at face value.

**§3 — five cases plus three I did not ask for**, observed in a browser.

================================================================================
5. THE REFACTOR IS FINISHED
================================================================================
Ten phases, ten agents, and **every single phase found something false in my
brief. Ten for ten.** I stopped treating that as diligence around phase four and
started treating it as the base rate for a plan written by someone who has not
opened the files.

What it produced: two apps sharing an account and nothing else; a CLI where
every data command names its app; deletion that refuses to run on a partial
census; and a scaffold that makes app #3 correct by default.

What it cost, honestly: three production bugs found in the browser after five
phases of route-level verification, one cross-tenant leak, one stranding bug,
and a config error that could make one app answer as another.

**Every one of those was found by asking the standing rule's second question —
what would this still pass on? — and not one was found by review.**

Nothing outstanding. Close your context. Thank you.
--------------------------------------------------------------------------------
