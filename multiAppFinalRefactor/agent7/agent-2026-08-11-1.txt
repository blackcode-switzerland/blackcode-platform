FROM:    agent7
TO:      master
DATE:    2026-08-11
PHASE:   7 — The scaffold and the docs. The last phase.
STATUS:  REPLY — DONE, full gate green, three commits, not pushed.
         ** THE BROWSER PASS FOUND THREE THINGS LIVE IN PRODUCTION AND ONE OF
            THEM IS A FALSE PRODUCT CLAIM ON THE PUBLIC LANDING PAGE. §2. **
--------------------------------------------------------------------------------

SUMMARY

The scaffold owns its tenancy: an app copied from it signs people up, mints them
a workspace, shows them their team and invites colleagues, with no issues app in
the story. The docs describe that instead of the pre-refactor world. And the
browser pass — the phase's headline — found more than the pages it was pointed
at.

Six things to read even if you skip the rest:

  1. §2 — ** THE PAGES TELL PEOPLE TO RUN COMMANDS `bk` NO LONGER HAS. ** Step 3
     of the public quickstart, three server `suggestion` strings an AGENT acts
     on, and `bk undo` sold as a feature over a journal that never existed.
     Phase 4 updated every surface the CLI owns and nothing could have noticed.
  2. §5.1 — ** I NEARLY REPORTED A FALSE FINDING. ** I wrote down "silent
     no-op", four times, because my instrument could not see a 4-second toast.
     The mechanism generalises past me and is the most useful thing I learned.
  3. §1 — the scaffold, and the ONE THING I DID NOT BUILD, stated plainly so it
     visibly is not finished rather than looking finished (your §6.1).
  4. §4 — ** `verify.sh` COULD NOT SEE A NEW APP'S TABLES. ** The instrument
     CLAUDE.md calls the only thing standing between this refactor and losing
     somebody's comments reported a clean bill on five brand-new tables by never
     asking about them.
  5. §5.2 — my own migration was wrong and exited 0. CLAUDE.md finding #20.
  6. §6 — what is half-done and unwritten, which you asked for as the real
     deliverable.


================================================================================
1. THE SCAFFOLD: WHAT IT HAS, AND WHAT A COPY INHERITS
================================================================================
An app copied from `apps/_scaffold` today gets:

  * `scaffold.workspaces` / `workspace_members` / `invitations` (migration 0003),
    and `notes` + `note_counters` repointed off `platform.workspaces`
  * `AppContext.workspaces` supplying its OWN source — required, not optional
  * `POST /api/auth/register` carrying the whitelist gate, with the test
  * `lib/auth.ts`, `lib/auth/session.ts`, the NextAuth handler, a `/login` page
  * a workspace + owner membership minted in ONE transaction on first sign-in,
    idempotent, best-effort, self-healing
  * a members page at `/dashboard`
  * `bk scaffold invite send|list|revoke` and its three routes
  * no `requireAppAccess`, no `workspace_apps`, no `app_access` — all gone

TypeScript names stay PREFIXED (`scaffoldWorkspaces` → `scaffold.workspaces`), per
your §1. `schema.ts` re-exports the platform schema, so a bare
`export const workspaces` shadows `platform.workspaces` silently at every import
site. Agent 2's finding; agent 4 credited it as the reason a 35-call-site move
stayed readable.

--- THREE THINGS I REMOVED OR DECLINED TO BUILD, EACH A DECISION ---------------

(a) ** THE ENTITY PROJECTION IS GONE FROM THE SCAFFOLD. ** It still had
    `lib/db/queries/entities.ts`, `scripts/reproject.ts` and a `projectEntity`
    call wired inside `createNote`'s transaction — i.e. the scaffold would have
    made app #3 the SECOND writer of `platform.entities`, one phase after you
    made sales stop. Your brief does not mention it. The URN is unaffected: it is
    built from the app's own slug and #number, which is what always made it true.

(b) ** NO `scaffold.uploads`, AND NO `platformUploadLedger` EITHER. **
    `AppContext.uploads` is required, and the scaffold serves no upload route.
    Pointing it at the platform ledger teaches the coupling Phase 3 removed;
    creating a table nothing writes is agent 2's `deletion_batches` shape. So it
    is a value whose two methods THROW with a message naming
    `apps/sales/lib/db/queries/uploads.ts`. Required stays required, the answer
    is honest, and mounting an upload route without doing the work fails loudly
    on the first request.

(c) ** NO DASHBOARD CHROME, AND THIS IS THE §6.1 ANSWER. ** See §7.


================================================================================
2. THE BROWSER PASS — PER PAGE, AND WHAT IT FOUND
================================================================================
Two dev servers (issues :3200, sales :3100), one real signed-in session
(balathanusan+1@blackcode.ch) against real local data. Ports: 3000 was held by an
unrelated project's dev server belonging to another user — I did not touch it.

--- 2a. ISSUES -----------------------------------------------------------------
  dashboard (projects)   RENDERS. 3 projects grouped by status, sidebar counts
                         (Projects 3 / Tasks 3 / Issues 16 / Labels 0 /
                         Members 2 / Inbox 1), List/Kanban/Timeline, filters.
  an issue with comments RENDERS. #16, four comments: headings, code spans,
                         tables, blockquotes, nested lists all round-trip. The
                         stored `<script>alert(document.cookie)</script>`
                         renders as TEXT — the sanitizer works.
  labels                 RENDERS (empty state), and I created one through the
                         UI; listing went 0 → 1, and it attached to an issue.
  a project              RENDERS. #1 "Hello": updates, description editor,
                         tasks 1, issues 1, activity, comment box.
  settings → workspace   RENDERS. `/dashboard/settings/workspace` is a
                         deliberate redirect to `/dashboard/workspaces`; the
                         per-workspace Manage page shows Logo / Name / Storage /
                         Danger zone. ** This is the page agent 6 changed and
                         explicitly did not look at. The Apps panel is cleanly
                         gone — no orphan heading, no empty section. **
  settings → members     RENDERS. 2 members with roles and joined dates.
  search                 ** ISSUES HAS NO GLOBAL SEARCH PAGE. ** The browser
                         surface is the per-listing filter, which works (16 → 1
                         on "Rich-text"). `/api/workspaces/{ws}/search` exists
                         and is `bk issues search`'s route. Not a bug; your list
                         implies a page that does not exist.
  trash                  RENDERS. I created an issue, binned it (the
                         `useConfirm()` dialog appeared — "Delete #18? It will be
                         moved to Trash"), saw it listed with deleted-by, and
                         restored it. Full cycle.
  upload a file          WORKS END TO END. Slash menu → Attach file → ledger row
                         19 in `platform.uploads` with `app='issues'` under
                         `/uploads/issues/balathanusan-1/…` (the per-app prefix
                         from Phase 3), AND the blob-reference trigger fired:
                         `platform.blob_references` gained the row with
                         `app=issues, source_type=issue, source_id=732`.

--- 2b. SALES ------------------------------------------------------------------
  dashboard              RENDERS, fully, with data. ** This is agent 6's fix
                         holding: the page that 404'd for four phases. ** Stat
                         tiles, 5 upcoming meetings, 5-item pipeline queue.
  a prospect detail page RENDERS. #1 StaffUp: URN
                         `bc:sales:balathanusan-1-2/prospect/1` printed, deal
                         journey, contacts, objections, triangulation. ** The
                         "Related in other apps" panel agent 4 deleted is gone
                         and left no hole. **
  pipeline               RENDERS. Table (15 rows) and Board across all six
                         stages.
  meetings               RENDERS. 10, with status, participants and notes.
  settings → members     RENDERS — the fix in 24875e8 holds. The members list is
                         there, AND I created an invitation and watched the
                         pending list render it, which is the half that "silently
                         rendered nothing" and which an empty state cannot prove.
                         ** But see 2c FINDING 4. **
  settings → tokens      RENDERS. 2 existing tokens with prefix + last-used; I
                         created one and the "Copy it now — this is the only time
                         the token is shown" panel appeared.
  trash                  RENDERS. Empty at first; I binned a test prospect
                         through the API and the table rendered it
                         (Ref/Title/Deleted/By/Purges), then restored it.
                         ** Sales' UI cannot bin anything binnable ** — a contact
                         is deliberately not a trash type (no #number, and
                         `trash-types.test.ts` asserts it), so the bin is
                         agent-written by design.
  upload a file          ** SALES HAS NO BROWSER UPLOAD PATH ** — "Documents are
                         maintained through `bk sales upload | doc create`". Via
                         the API: the file landed in `sales.uploads` under
                         `/uploads/sales/balathanusan-1-2/…`, and
                         `platform.uploads WHERE app='sales'` is still 0. Phase 3
                         verified end to end.

--- 2c. WHAT IT FOUND ----------------------------------------------------------

** FINDING 1 — THE PAGES NAME COMMANDS THE BINARY NO LONGER HAS. FOURTEEN OF
   THEM, ACROSS NINE FILES, AND THE SHARPEST IS LIVE ON THE PUBLIC SITE. **

Phase 4 re-tiered ten verbs and removed `bk link`. It updated the CLI, the
deprecation rows and the guide topics — every surface the CLI owns. Nothing could
have noticed the rest: `guide_test.go` reads `cli/internal/guide`, and a React
component is not a guide topic.

    apps/issues landing page, step 3 of the quickstart:  bk workspace use my-team
    the same page's FAQ, twice:                          bk activity, bk trash list
    the login page's demo terminal:                      bk issue create
    apps/sales search page, twice:                       bk search  (cross-app)
    apps/sales activity page:                            bk activity --app sales

The quickstart one is the one I would put first: it is step 3 of the
getting-started path on a public marketing page, so **the first command a new
user is told to type answers `unknown command "workspace"`.** The deprecation
hint rescues them — which is precisely why nobody noticed, because the recovery
path was doing the onboarding's job.

`bk activity --app sales` is wrong twice over: the verb moved AND `--app` was
removed in the same release.

The sales search copy is worse than a stale spelling: it promised a capability
that PLAN.md §3 deliberately dropped. "To find something by name across every
blackcode app, that is `bk search`" — there is no such thing, and the no-results
state repeated it as the recovery advice shown exactly when somebody found
nothing.

** AND THREE SERVER `suggestion` STRINGS, WHICH MATTER MORE THAN PAGE COPY. **
CLAUDE.md: the CLI prints a suggestion as the `hint:` line, and it is what turns
a dead end into a recovery. These told an agent to run:

    bk workspace list --all   (blob-drift + entity-drift — and `--all` was itself
                               removed in YOUR Phase 5)
    bk member list            (two sales prospect routes)
    bk invite revoke <id>     (sales invitations)

** FINDING 2 — `bk undo` IS ADVERTISED AS A FEATURE ON THE LANDING PAGE. **
A card titled "Reversible edits", `preview` pill: *"Issue updates are journaled
with full before/after snapshots. `bk undo` reverses your last few changes."*
Plus an architecture bullet ("rolls back your last writes for real — it is not a
UI trick") and an FAQ answer.

There was never such a journal. `platform.transaction_log` had no writer, which
is why the command went in CLI 1.12.0 and `/api/undo` is a 410 — and **agent 6
dropped the table on 2026-08-10**. The public site has been selling a capability
that did not exist, under a pill that made "not working yet" the reading. Card
removed; Trash is the real version of that promise and has its own card.

** FINDING 3 — SALES' MEMBERS PAGE DID NOT ASK ABOUT `ui_mode`. **
`read_only` is the DEFAULT mode. `member-settings.tsx` was the ONE component in
the app that rendered record-write affordances without calling `useCanWrite()`,
so out of the box it showed a live-looking Invite field and enabled
Remove/Revoke, while the prospect page one click away said "Editing is hidden —
this browser is in read-only mode".

Nothing was ever written and the refusal was LOUD — see §5.1, which is the part
worth your time. That is why it survived: the safety net held and the affordance
stayed wrong underneath it. `lib/read-only.test.ts`'s own header disclaimed this
property in as many words ("What this does NOT claim: that every button is
correctly hidden"). It now asserts the checkable version of it.

** FINDING 4 — A PENDING INVITATION'S LINK WAS UNRECOVERABLE AFTER A RELOAD. **
The section says "b/sales does not send email — copy the link and send it
yourself". The link appeared only in the banner shown immediately after inviting.
After a reload the owner's options were revoke-and-reinvite, or read the token
out of Postgres — **and the token was already in the row's payload**, which is
what made the gap invisible. It is on every row now.

** FINDING 5 (minor, pre-existing) ** — hydration mismatch on the issues login
page's theme-toggle `aria-label` (next-themes SSR, dev only). The `useConfirm()`
dialog carries no `role="dialog"`/`alertdialog`. Sales' trash "By" column shows
`—` for a token-authenticated delete. None fixed; none caused by this refactor.

--- 2d. THE GUARD, BECAUSE A BROWSER PASS IS NOT REPEATABLE --------------------
`packages/platform-testing/test/retired-cli-spellings.test.ts`. It **derives** the
retired spellings from `deprecations.go` rather than listing them (finding #9's
trap), strips comments so text that DISCUSSES a removal is not flagged, and
asserts the stripper did not eat everything.

It found more than I did: `bk undo` ×4, `bk issue create` on the login page, and
the three server suggestions — none of which was on the pages your §5 listed.
Fourteen fixed, ten allowed with reasons (the `/api/undo` 410 has to name the
command it replaces).


================================================================================
3. EVERY DOC I CHANGED, AND THE CLAIM IN IT THAT WAS MOST WRONG
================================================================================
docs/adding-an-app.md   Most wrong: **"Copy `apps/_scaffold`. It is a real app:
                        … an entity projection and its reconciler"** — the file
                        telling people to copy the one thing this refactor spent
                        six phases removing. Rewritten around the contract table;
                        three tiers → two; the boundary rules now say
                        `platform.*` is shared IN NAME and only partly in fact;
                        the browser check added as a required step; the
                        STEPS-8-AND-9 box left SIGNED with a dated note.
CLAUDE.md               Most wrong: the same scaffold sentence. Plus finding #20
                        and a fifth corollary ("a route is not a page").
docs/backend.md         Most wrong: **"`appSlug` | the identity `requireAppAccess`
                        checks against"** — defining a required field by a
                        function agent 6 deleted. Also `WorkspaceSource` is six
                        methods, not seven.
docs/sales-app-plan.md  Dated note, file kept (~50 files cite it). D-11, D-28,
                        D-36. D-28 is the one worth reading: it did not get
                        overruled, it **expired on its own stated terms**.
apps/sales/docs/frontend.md  Most wrong: "Inviting and removing go through
                        `lib/mutations.ts` behind `useCanWrite()`" — true of the
                        mutation layer, false of the affordances, which is
                        exactly the gap in finding 3.
apps/_scaffold/app/api/README.md  Rewritten: the fourth shared factory not to
                        mount, and — new — what IS safe to mount and why.
docs/changelog/{platform,sales}.md   Dated entries.

** ONE DOC CLAIM I COULD NOT VERIFY AND SOFTENED RATHER THAN REPEATED: **
`docs/adding-an-app.md`'s proof 9 ("signed in at issues, opened sales, already
signed in"). It is true and it proves the cookie is shared. It does NOT prove the
person can use the second app, and after Phase 2 they usually cannot — see §6.


================================================================================
4. ** THE LEDGER COULD NOT SEE A NEW APP **
================================================================================
`multiAppFinalRefactor/lib-db.sh` hardcoded:

    WHERE table_schema IN ('platform','issues','sales')

So the scaffold's five brand-new tables — `scaffold.workspaces`,
`workspace_members`, `invitations`, `notes`, `note_counters` — were reported
CLEAN by `verify.sh` because it never asked about them. The instrument CLAUDE.md
calls *"the only thing standing between this refactor and losing somebody's
comments"* does not, by default, see a new app.

It is `TRACKED_SCHEMAS` now, with a header saying it is a list you extend, and a
step in `docs/adding-an-app.md`. WATCHED: before, five tables invisible; after,
five tables named individually as undeclared.

** I DELIBERATELY DID NOT WIDEN IT TO EVERY SCHEMA, and I want the reason on the
record: ** that would pull in `public`, `drizzle`, `neon_auth` and anything an
extension creates, and each would then read as "in the database but NOT in the
baseline" against every existing baseline — including `baseline.txt`, at the
moment the human is running it to check a production deploy. A safety instrument
that cries wolf on its first run after a change is one people learn to ignore.
Against production the change is inert: there is no `scaffold` schema there.

** AND `baseline.local-example.txt` IS STALE, IN WAYS THAT ARE NOT MINE. **
verify.sh reports 6 pre-existing failures against local dev:

    workspace_apps / app_access / transaction_log  — present in the baseline,
      dropped by agent 6's 0045. Agent 6 BUILT the `DROPPED` declaration
      mechanism and did not apply it to this file.
    platform.uploads  -1 vs declared -2
    platform.events   -43 vs declared -54
    platform.entities -63 vs declared -66

** I did not re-capture it, and that is a decision. ** Agent 5 §9.2 destroyed 3
`platform.events` rows in local dev and said the residual would show here; agent
6 left probe rows on purpose. Re-capturing would erase the only trace of both.
`baseline.txt` — the production one — is untouched and is the human's to
re-capture after deploy.


================================================================================
5. WHAT I WATCHED FAIL — AND THE ONE I GOT WRONG FIRST
================================================================================

5.1 ** I NEARLY WROTE DOWN A FINDING MY INSTRUMENT COULD NOT MAKE. **
    I reported, to myself, four times, that clicking Revoke on sales' members
    page in read-only mode was a SILENT no-op: no toast, no request, no change. I
    had two full-page screenshots and a 5-second `wait_for` that timed out.

    It is not silent. `useRecordMutation` throws `ReadOnlyModeError` and raises a
    toast naming both recoveries. I proved it by instrumenting the page —
    clicking and polling the toaster inside ONE `evaluate` — instead of sampling
    it between tool calls:

        toastTexts: ["This browser is in read-only mode. Switch to full mode in
                      Settings → Preferences, or write with `bk sales`."]

    ** A browser driver whose round-trip is a few seconds cannot observe a
    4-second toast. ** Every sample lands after it has gone, and the absence
    looks like evidence. This is agent 5's §5.3 in a new medium — "an injection
    you have not asserted landed is not an injection" — except here it was an
    OBSERVATION I had not established my instrument could make.

    The finding survived, narrower and correctly stated: the affordance is wrong,
    the safety net works, and the net working is why it lasted four phases. But I
    would have shipped "silent failure" into your file, and you would have had no
    way to know. It is in `docs/adding-an-app.md`'s browser section so the next
    person does not repeat it.

5.2 ** MY OWN MIGRATION WAS WRONG AND EXITED 0. CLAUDE.md FINDING #20. **
    0003's FK swap was guarded on `notes_workspace_id_workspaces_id_fk` —
    Drizzle's spelling. Postgres had called it `notes_workspace_id_fkey`, because
    0001 is hand-written SQL with an inline `REFERENCES` clause. So the DROP
    matched nothing, the ADD succeeded, and `scaffold.notes` ended up carrying
    BOTH foreign keys: a row then had to satisfy `platform.workspaces` AND
    `scaffold.workspaces` at once — strictly worse than the coupling the
    migration exists to remove. Every CREATE looked right and psql exited 0.

    Found by reading `pg_constraint` after running it. It matches on `confrelid`
    now, which is also what makes it correct for a copy whose 0001 was
    drizzle-generated. ** Fourth time on this project that the catalog
    contradicted the code ** — agent 1's trigger, agent 3's twelve FKs, agent 4's
    cascade ordering, and this.

5.3 THE WHITELIST GATE, LIVE AND AS A TEST. Your §2 says watch it refuse rather
    than reason that it would. Live against the scaffold on :3300:
        stranger@example.com          -> 403 not_in_whitelist
        agent7-scaffold@blackcode.ch  -> 201, and the ROWS existed
    And three injections into the route, each asserted to have landed:
      (a) refusal block deleted   -> the two denial cases RED, both positive
                                     cases GREEN (the discrimination working)
      (b) `if (!allowed)` inverted-> ALL FOUR RED
      (c) gate moved below the
          existence check         -> ONLY the ordering case RED

5.4 THE NEW `retired-cli-spellings` GUARD, three ways:
      * the real bug restored (`bk workspace use my-team`) -> RED, naming it
      * the Go declaration renamed -> the PREMISE case RED, not a silent green
        over an empty spelling list
      * the comment stripper made to blank every file -> the POSITIVE case RED
        ("still sees rendered bk commands"), which is finding #16's lesson: a
        scan whose subject vanished reports success

5.5 THE NEW `read-only` CASE, three ways: the real bug restored; the declared
    parent stopping gating (the declaration becomes false); the import spelling
    broken (the input assertion). All RED.

    It also caught something on its first run that I had got wrong by grepping:
    `prospect-forms.tsx` appeared to consult `useCanWrite` and does not — the
    only mention is in its own HEADER. The comment-stripping is what discriminated.

5.6 THE ROLLBACK. Ran it, and my header's claim ("FAILS LOUDLY if a note would be
    orphaned") was UNWATCHED at first — the first run succeeded only because
    `scaffold.notes` was empty. Created a note against a scaffold workspace with
    no platform counterpart, re-ran: psql exit **3**, and BEGIN/COMMIT meant
    nothing was left half-done.

5.7 THE MIGRATION applied twice more (idempotent, exit 0 both times), and the FK
    swap read back out of `pg_constraint` with ON DELETE CASCADE preserved on
    both tables and ZERO remaining FKs on `platform.workspaces`.

5.8 THE BOOTSTRAP SELF-HEALING, which is a claim I put in three headers: dropped
    the tenancy tables out from under a signed-in account, re-applied, signed in
    again — the workspace came back.


================================================================================
6. WHAT IS HALF-DONE AND UNWRITTEN — your §5's last question
================================================================================
As the person who has now read all seven phases:

6.1 ** ARRIVING ON ANOTHER APP'S COOKIE GETS YOU A SESSION AND NO WORKSPACE, AND
    NOTHING RESCUES YOU. ** The session is shared across `*.blackcode.ch` (D-16),
    so somebody signed in to issues opens sales already authenticated. But
    membership is per app, and `ensureWorkspaceForUser` runs in the SIGN-IN
    callback — which they never triggered there. They land on "no workspace yet"
    and the only way out is to sign out and back in.

    ** This is production behaviour today, not a scaffold artefact **, and I
    found it by walking the scaffold rather than by testing sales. It also makes
    `adding-an-app.md`'s signed proof 9 read stronger than it is: "already signed
    in" proves the cookie is shared, not that the person can use the app.

    Three candidate answers, none taken: bootstrap on first authenticated request
    (cheap, but any app then silently mints tenancy for anyone with an account);
    keep the empty state and add a "get started" button calling the bootstrap;
    or decide a new app is invite-only and the empty state is correct. Written up
    as an open item.

6.2 ** `workspace_invitations.app` HAS NO READER (agent 6 flagged this for me and
    I did NOT close it). ** New rows are NULL, historical rows kept. It is
    `transaction_log`'s shape — a column with no writer or reader — and the way
    that one ended was a phase stopping to decide whether it was safe to drop. I
    left it because it is a `platform.*` change and your §4 says no.

6.3 ** `error_events.workspace_id` — still no writer, 0 of 328 rows. ** Agent 6
    found the `app` column's stated justification was wrong. Give it a writer or
    drop it; either is a decision, and leaving it is how it becomes a third one.

6.4 ** `WorkspaceSource.getById` STILL HAS NO CALLER. ** Agent 4 §9.5 and agent 5
    §9.6 both flagged it. I did NOT delete it — and I have now made it worse by
    adding a third implementation of it in the scaffold. A required interface
    method that every app implements and nobody calls is the friendly shape of
    the problem CLAUDE.md is about. It is a five-line deletion across three apps
    and two fakes.

6.5 ** THE `bk undo` CLASS OF PROBLEM IS NOT CLOSED. ** I removed the landing
    page's claim, but the guard I wrote only catches a page naming a retired
    COMMAND. It cannot catch a page describing a retired CAPABILITY in prose. The
    marketing copy has not been audited against what the product does since the
    apps split, and `bk undo` was there for months.

6.6 ** `SET NOT NULL` on `error_events.app` ** — deferred, still deferred,
    correctly. Production has 0 rows saying `sales`, which is the precondition
    working, not failing.

6.7 THE LOCAL BASELINE — §4. Stale in six tables, none of them mine, and the
    staleness encodes agent 5's destroyed rows. Somebody should decide whether to
    re-capture (losing the trace) or to declare (keeping it).


================================================================================
7. WHERE YOU WERE WRONG, AND WHERE I WAS — your §6, item by item
================================================================================

7.1 ** §6.1, "how much of the scaffold this actually is" — YOU WERE RIGHT TO
    ASK, AND MY ANSWER IS A SPLIT RATHER THAN A YES OR NO. **

    The SERVER half is done and is what "app #3 is born correct" actually
    required: tenancy, the gate, the bootstrap, the invitation surface, the CLI,
    the guards. That is comparable to agents 2+3 and it fitted, because they had
    already made every hard decision and I was copying settled shapes.

    ** THE BROWSER HALF I DELIBERATELY DID NOT BUILD, and you should know exactly
    what is missing. ** The scaffold has a `/login` page and ONE page behind it —
    a server-rendered members page. It has no dashboard shell, no nav, no theme
    provider, no query client, no toast library, and it depends on none of those
    three packages. Sales has ~25 dashboard directories.

    Why: a scaffold that ships a design is a design every copy has to undo, and
    the actual failure your brief cites — the middleware/cookie bounce — was
    about the scaffold not having the AUTH SHAPE, which it now has. I judged that
    inventing a UI framework for a scaffold would make it a bigger thing to copy
    without making app #3 more correct.

    ** It visibly is not finished rather than looking finished ** (your words):
    the members page's header says what it is and is not, and
    `adding-an-app.md`'s "what genuinely cannot be copied" section says the
    scaffold ships no design and points at `platform-ui`. If you disagree, this
    is the decision in the phase I would most want reviewed — it is additive, and
    nothing I built has to change.

7.2 "§6.2, rewrite or re-derive `adding-an-app.md`." REWRITE, and the reason is
    concrete: about 70% of that file is hard-won and still true — the `@source`
    line, the blob triggers, the boundary probe's `SET ROLE` trap, the signed
    box. Re-deriving would have thrown away four findings that cost real
    incidents. What I rewrote is the contract, the tiers, the boundary rules and
    the open items, and where a section is superseded it SAYS WHAT REPLACED IT
    rather than vanishing — a reader who finds only new text re-derives the old
    design from whatever sentence survived elsewhere.

7.3 "§6.3, I do not know what is stale across the doc tree." Less than you
    feared. Agents 4, 5 and 6 did their docs properly — `platform-architecture`
    §4.5, `backend.md`'s dropped-table sections and sales' docs were already
    annotated or struck through. **The stale things were not in the docs the
    phases touched; they were in the ones nobody thought of as docs** — marketing
    copy, a login page's demo terminal, and three server `suggestion` strings.

7.4 "§6.4, the browser check may find more bugs." It did, and the ones that
    mattered were not on the pages you listed. Your list pointed at sales'
    members page and issues' settings — both fine. The findings were on a
    marketing page, a login page, a search page and an activity page.

7.5 ** "§6.5, five times the thing that mattered was outside where anyone
    looked." SIX. ** This phase's was the same shape and a new place: the CLI's
    advertised surface had moved, and the copy describing it lived in React
    components and in `Errors.badRequest(...)` third arguments — neither of which
    is a route, a guide topic, or a doc.

7.6 ONE THING YOUR BRIEF ASKED FOR THAT I DID NOT DO: §3 lists
    `docs/platform-db.md` among the docs to update. I read it and found nothing
    stale — agent 2 and agent 3 had already added `error_events.app`, migration
    0004, the sequence offset and the `active_workspace_id` section. I made no
    edit rather than an edit for its own sake. Flagging it so "not changed" is
    not read as "not checked".

7.7 SOMETHING I DID THAT YOU DID NOT ASK FOR, so you can overrule it:
    `appverbs.Config` gained `InviteCandidates` and `InviteAccept`. Without it,
    `Invites` mounted seven subcommands and the scaffold has files for three, so
    it would have claimed four routes that can only 404. `bk issues invite` and
    `bk sales invite` are byte-identical (both set all three flags, verified).
    Reverting is deleting two fields and three `if`s.

--------------------------------------------------------------------------------
THE GATE, from the repo root:
  npm run typecheck            10/10
  npx turbo test --force       4/4 — issues 252/76 skipped, sales 88/8,
                               scaffold 15, platform-testing 42
  npm run lint                 0 errors, 9 warnings (all pre-existing)
  npm run build                3/3
  cd cli && go build / vet / test    all green
  cd cli && make routes        RUN. +18 lines — the scaffold's three invite claims
  verify.sh                    §4. 6 pre-existing failures, none mine, all named

THREE COMMITS, local, not pushed:
  8ab0dc7  fix(web): the pages told people to run commands bk no longer has
  299e5d6  feat(scaffold): the scaffold owns its tenancy, so app #3 is born correct
  dd704d3  docs(refactor): the checklist, the contract, and the sentences that expired

LOCAL DEV, so the next person is not surprised:
  * the `scaffold` schema now EXISTS locally (0001–0003 applied). It did not
    before — the scaffold had never been migrated on this machine.
  * probe rows, all clearly named: `platform.users` 121 (agent7@blackcode.ch,
    via issues) and 122 (agent7-scaffold@blackcode.ch), `scaffold.workspaces` 1
    (agent-7-scaffold), one issues label `agent7-browser-pass`, one issues issue
    #18 in `balathanusan-1`, two uploaded probe files, one sales API token
    `agent7-browser-pass`. I did NOT delete them: cleaning up probe rows is the
    operation that destroyed data on this project once already (agent 5 §9.2).
  * `apps/_scaffold/.env.local` created (gitignored, confirmed). It needs the
    SAME `NEXTAUTH_SECRET` as the other two — on localhost the apps share a
    cookie jar because cookies ignore the port, so a different secret gives you
    `JWEDecryptionFailed` from the first app's cookie with no obvious cause.
  * `bk` was run ONLY under `BK_CONFIG_DIR=/tmp/bk-agent7-cfg`. No production
    credential was held or requested; every server was localhost.

WHAT I COULD NOT VERIFY:
  * Anything in production or on Neon.
  * A real Vercel Blob callback — local dev writes to `public/uploads`, so the
    per-app prefix I observed came from the multipart branch. Same gap agents 4,
    5 and 6 report.
  * Google sign-in. Same as agents 3, 4, 5 and 6.
  * The scaffold's members page with more than one member, or an invitation
    ACCEPTED — the scaffold serves no accept route by design (§7.7).

WHAT I NEED FROM YOU: a ruling on §7.1 (the browser half of the scaffold, not
built) and on §7.7. Everything in §6 is a decision somebody has to take and none
of them is mine.
--------------------------------------------------------------------------------
