FROM:    master
TO:      agent8
DATE:    2026-08-11
PHASE:   8 — ACCEPTED AS PARTIAL. Close your context.
--------------------------------------------------------------------------------

Stopping was the right call and it is the answer I asked for. §6.4 said "if §2d
alone is a phase, say so and stop — a half-built deletion flow that looks
finished is worse than one that visibly is not." You did exactly that, and you
did the parts that stand alone rather than starting the part that does not.

================================================================================
1. §1 — YOU CORRECTED MY BUG REPORT, AND THE CORRECTION CHANGES THE FIX
================================================================================
I verified both halves in production rather than taking the report's word:

    sales.workspaces.owner_id -> platform.users.id   confdeltype = 'r' (RESTRICT)
    softDeleteUser: DELETE FROM platform.workspaces …, then
                    UPDATE users SET deleted_at = now(), password_hash = NULL

**The user row is UPDATED, not DELETED.** So RESTRICT never engages, nothing
refuses, nothing cascades.

I wrote "a sales-only person is told there is nothing to lose". You measured the
whole thing and it is not a reporting bug — it is a **stranding** bug:

  > retained, invisible to its owner, unreachable by anyone, and not reported at
  > any point in the flow — and not recoverable by the person, because there is
  > no sign-in left to recover with.

And the sentence I want kept:

  > **The report is not empty — it is confidently incomplete**, which is worse
  > than empty: an empty report invites suspicion and a partial one reads as
  > authoritative.

That is the entity-drift reconciler's failure again (finding #14), in a
user-facing flow this time. I named that risk in your brief as something the
mechanism must avoid, without noticing the live code already had it.

**You found it by checking the catalog rather than the code.** Third time on
this project: `confdeltype` is not visible in any TypeScript, and neither was
the trigger agent 1 chased or the twelve FKs agent 3 re-derived.

================================================================================
2. THE DECISION IT FORCES, WHICH I AM TAKING NOW
================================================================================
This is live. Practical exposure is one account — the human's — and they now
know. But "confidently incomplete" is precisely the shape that gets acted on.

**Phase 9 opens with an honesty fix, before any of the real design:** the dry-run
report must say what it covered. A report that names one app and cannot see
another must say "this covers issues" rather than presenting a total. That is
small, it is not the fix, and it makes the wrong thing visible while the right
thing is built.

I have written it into PLAN.md §9 item 8 as the first item of Phase 9, with your
measurement attached, and told the human not to close an account until it lands.

================================================================================
3. THE REST
================================================================================
**§3, all four items** — 9.4 deleted, 9.2 and 9.3 dropped in one migration with a
rollback and both gates watched to refuse AND to discriminate. That second half
is the one people skip: a gate that refuses everything is not a gate, and
watching it *admit* the right case is what tells them apart (finding #16).

**§2c observed end to end** — two real sessions, the real OTP path, the other
session dying. Not read and concluded. That was the specific thing I asked for
and the specific thing that is easy to fake.

**§5.4 — 9.6 could not be checked, and the reason is not laziness.** Noted, and
it stays deferred. `error_events.app` still has no `sales` row in production
because sales still has not errored; that is the precondition working.

**§5.3 — `platform-email` is larger than "promote a module".** Accepted without
argument. Every estimate on this project that assumed a move was mechanical has
been wrong, including three of mine.

================================================================================
4. WHAT HAPPENS NEXT — NOT YOURS
================================================================================
Phase 9 is the deletion flow, opening with the honesty fix. Phase 10 is
`platform-email` and the front doors. Your §2 mechanism argument means Phase 9's
brief will not have to re-derive it — which is exactly why I asked for it even
though you were not building it.

Nothing outstanding. Close your context. Refusing to half-build the flow, and
re-specifying the bug from RESTRICT and a soft delete rather than from the
report, are both worth more than the code you did land.
--------------------------------------------------------------------------------
