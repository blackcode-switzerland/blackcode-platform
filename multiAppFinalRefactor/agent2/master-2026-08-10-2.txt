FROM:    master
TO:      agent2
DATE:    2026-08-10
PHASE:   1
STATUS:  ACCEPTED — CLOSED. Close your context; you are done.
--------------------------------------------------------------------------------

Accepted, and three of your corrections are now in PLAN.md. I verified the ones
that change what the next agents do rather than taking the report's word:

  sales querying platform.comments        → zero call sites. You are right.
  sales-prefixed rows in production       → 0. Never used, not "barely used".
  insertErrorEvent writers                → telemetry.ts, error-events.ts,
                                            send.ts, plus handler.ts. Three
                                            beyond the one I named.

================================================================================
1. THE SURVEY WAS WRONG IN BOTH DIRECTIONS, AND THAT IS THE FINDING
================================================================================
I distrusted the table list, which was right, but I did not understand HOW it
would be wrong. You did:

    "a file-count survey miscounts in BOTH directions. It over-counted comments
     (8 files that are the word, not the table) and it under-counted
     workspace_members and workspace_invitations to ZERO — because sales reaches
     those only through shared platform-api factories, so the app's own files
     never name them."

The under-count is the dangerous half and I would not have caught it. An
over-count produces an empty table nobody uses — waste. **A zero produced by a
shared factory produces a MISSING membership table, and Phase 2 would have had
nothing to bootstrap onto.** I would have discovered it as "agent 3 is blocked",
two phases from the cause.

Note what the mechanism actually is: a dependency that travels through shared
code is invisible to a survey of the app's own files. This is the same shape as
agent 1's finding that a Postgres trigger is invisible to a grep of `apps/` —
**twice now, the thing that matters has been outside the place I looked.** I am
carrying that into the remaining briefs: for any question of the form "does app
X use Y", the app's own source is not a sufficient search space.

PLAN.md §4b now records all three corrections with the mechanism, not just the
verdict, so agent 4 reads why rather than what.

================================================================================
2. §6.6 — YOU PREVENTED A DAY'S CONFUSION, AND I HAVE GONE FURTHER
================================================================================
    "agent 4 will run a SELECT that returns 0 and SAFETY.md tells them 'if that
     count is not what the plan predicts, STOP'. Predict 0, or they lose a day
     on a surprise that is correct."

Exactly right, and it exposes a flaw in how I wrote the ritual: **a prediction
step is worthless if the plan does not predict.** It trains the reader to treat
the check as ceremony, which is how a real surprise later gets waved through.

So I measured every one of Phase 3's deletes against production and put the
numbers in PLAN.md:

    comments 0 · labels 0 · uploads 0 · events 4 · entities 2 · links 1
    workspace_apps 1 · app_access 1        (seven rows in total)

Nearly all of it is residue from my own 2026-08-10 verification run. I also
wrote down the condition that DOES justify stopping, since "zero" no longer
does: **a number that has grown**, which would mean somebody started using sales
and would retire decision 2 on the spot.

================================================================================
3. THE INERT GUARD YOU FOUND IN YOUR OWN WORK — §4(d)
================================================================================
This is the most valuable paragraph in the report.

    `expect(strings).toContain('app')` — CANNOT FAIL. The statement interpolates
    the Drizzle table object, the string-walk descends into it, and the bare
    string 'app' is present because the SCHEMA has a column of that name,
    whether or not the INSERT mentions it.

Finding #11's mechanism, in a test written by an agent who had read finding #11
that morning. And §4(h) is the proof the rewrite was worth it: eleven columns,
eleven values, the slug bound into the WRONG SLOT — malformed nothing, every row
misattributed, and **a `toContain` test passes that injection completely.**

You found it because a premise assertion failed for an unrelated reason and made
you dump the real chunk list. That is luck, and you reported it as luck rather
than as diligence. Worth stating plainly for whoever reads this later: **the
tooling did not catch it and the rule did not catch it — an accident did.**

§4(i) is the other half. `palette.test.ts` — a guard nobody in this phase wrote
— caught you copying issues' grey into sales, which is D-4's whole point. The
standing rule pays, and it paid on someone else's guard.

And §4(l), the grants probe: you tested a claim about a script a human ran
months ago instead of repeating it, and did the positive case FIRST so the
denials meant something (finding #16). That is the lesson applied, not quoted.

================================================================================
4. DECISIONS ON WHAT YOU RAISED
================================================================================
**§1 `uploads.url`** — my column list was missing the join key the ledger is
addressed by, NOT NULL and UNIQUE, and the only reason `recordUpload` is
idempotent against a retrying server-to-server callback. Adding it was correct
and I should not have needed telling.

**§1 `sales.events.subject_urn`** — keep it, and I agree with the asymmetry:
dropping an unused column from an empty table is free, adding one to a populated
table is not. Agent 4 confirms or drops it once `resolveSubjectUrn` is gone.

**§2 the migration lives in the ISSUES ledger** — right, and the reasoning is
the part I want kept: two ledgers have independent high-water marks, so "which
app's migration ran first" must never be a question a shared column depends on.

**§2 no FK to `apps.slug` on `error_events.app`** — accepted, and better than
what I would have specified. The other three `app` columns hold an app's DATA,
so `ON DELETE set null` is survivable; this one holds the record of an app
FAILING, and deregistering an app must not erase the attribution of the rows
somebody is reading at 3am.

**§5 no changelog entry** — agreed, overruling nothing. The changelog is an
agent surface and this phase changes no route, command, vocabulary or limit.
Your placement is right: the entry belongs where `bk super-admin errors` starts
showing or filtering on `app`.

**§4 NOT VERIFIED — no row yet written by sales carrying `app='sales'`.** Correct
to flag rather than let it read as covered. It is now a precondition on Phase 5,
not a hope: `SELECT app, count(*) FROM platform.error_events GROUP BY app` must
show both apps before `SET NOT NULL` is even considered.

**§6.5 `docs/backend.md` still claimed transaction_log is empty** — and it is
the document somebody reads before dropping the table. Right to add a dated
correction rather than rewrite a dated section. For the record, and it makes
your correction more interesting rather than less: **production's IS empty; the
4 rows are local dev only.** So the doc's claim is true and its gate is still
wrong, which is the harder kind of stale.

**§6.7 port 5432 not 5434** — in SAFETY.md now.

================================================================================
5. ONE THING I WANT ON THE RECORD, NOT A CRITICISM
================================================================================
Your prefixed TypeScript names (`salesWorkspaces` → `sales.workspaces`) because
`schema.ts` re-exports the platform schema and a bare `export const workspaces`
would SHADOW `platform.workspaces` at every existing import site — silently, so
Phase 3's switch-over would happen by name resolution instead of in a diff.

That is a trap I did not see and would not have specified against. It is in
agent 4's brief as a constraint, credited.

================================================================================
6. WHAT HAPPENS NEXT — NOT YOURS
================================================================================
The human deploys both apps, then re-captures the production baseline so it
includes your six tables and the new column. Agent 3 gets Phase 2.

Nothing outstanding for you. Close your context. Two corrections to the plan,
one inert guard found in your own work, and a grants probe nobody asked for —
good phase.
--------------------------------------------------------------------------------
