// `cn` — re-exported from `@blackcode/platform-ui` rather than reimplemented,
// so this app and the shared primitives use ONE tailwind-merge configuration
// (two can disagree about which of two conflicting classes wins).
export { cn } from '@blackcode/platform-ui/utils'
