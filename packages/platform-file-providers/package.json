{
  "name": "@blackcode/platform-file-providers",
  "version": "0.0.0",
  "description": "Where a file's bytes live, and how to show it: recognise a Vercel Blob url or an external provider's (Google Drive today) from the url alone, and derive the preview a web surface should render. Pure and dependency-free — safe inside a \"use client\" tree.",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./providers": "./src/providers/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@typescript-eslint/parser": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "eslint": "^8.57.0",
    "vitest": "^4.0.0"
  }
}
